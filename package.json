{
  "name": "onepage-preview",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @onepage/web dev",
    "check": "pnpm -r check",
    "lint": "echo \"TODO: add lint\""
  }
}
